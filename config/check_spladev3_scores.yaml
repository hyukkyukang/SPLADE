# SPLADE-v3 distillation scores sanity check
defaults:
  - dataset: msmarco_spladev3_scores
  - _self_

sanity:
  max_rows: 1000
  log_every: 200
  sample_rows: 3
  strict: true

dataset:
  score_log_every: 100000
  score_progress_bar: true
  score_progress_refresh: 0.5
