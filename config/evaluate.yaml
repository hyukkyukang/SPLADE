# Evaluation entrypoint config
seed: 10
log_dir: "logs"
root_dir_path: "."

# Defaults
defaults:
  - _self_
  - model: splade_v1_sum
  - dataset: beir/msmarco
  - testing: default
  - override hydra/job_logging: colorlog_custom

hydra:
  output_subdir: null
  run:
    dir: ${log_dir}
