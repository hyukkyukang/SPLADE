# @package testing
defaults:
  - _base

name: patent_us_eval

batch_size: 32
num_workers: 8
torch_compile: true
torch_compile_mode: max-autotune
max_windows_per_forward: 160
scoring_workers: 8
exclude_self_match: true
k_list: [1, 5, 10, 16, 32, 64, 150]
